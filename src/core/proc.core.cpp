// Widh Jio
// proc.core.cpp

#include <thread>
#include <chrono>

#include "core.hpp"

/*void core::startSyaaAP(fdeep::model* model, WSServer* server)
{
  std::thread([=]() {
    for(;;)
    {
      std::this_thread::sleep_for(std::chrono::seconds(1));
      std::cout << "lovebug!" << std::endl;
      ws::broadcast(*server, keras::ch::SITDOWN);
    }
  }).join();
}*/
